import React from "react";
import ReactDOM from "react-dom/client";
import Index from "@/index";
import "@/assets/root.stylus";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Index />
  </React.StrictMode>
);
